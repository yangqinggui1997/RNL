import React from 'react'
import {View, StyleSheet, Text} from 'react-native'

const AccountScreen = () => {
    return <View style={{fontSize: 48}} >AccountScreen</View>
}

const styles = StyleSheet.create({})

export default AccountScreen
